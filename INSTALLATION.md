
# راهنمای نصب و راه‌اندازی روی سرور لینوکس (Ubuntu/Debian)

این راهنما فرض می‌کند شما یک سرور خام لینوکس (VPS) دارید و می‌خواهید این وب‌سایت را روی آن بالا بیاورید.

## مرحله ۱: اتصال به سرور
با استفاده از SSH به سرور خود متصل شوید:
```bash
ssh root@YOUR_SERVER_IP
```

## مرحله ۲: نصب پیش‌نیازها (Node.js و Nginx)
ابتدا مخازن را آپدیت کرده و ابزارهای لازم را نصب کنید:

```bash
# آپدیت سیستم
sudo apt update && sudo apt upgrade -y

# نصب Nginx (وب سرور)
sudo apt install nginx -y

# نصب Node.js (برای بیلد کردن پروژه)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# بررسی نصب صحیح
node -v
npm -v
nginx -v
```

## مرحله ۳: آپلود فایل‌های پروژه
فایل‌های پروژه را روی سرور کپی کنید. می‌توانید از FTP (مانند FileZilla) استفاده کنید یا اگر پروژه روی گیت‌هاب است، آن را کلون کنید.
فرض می‌کنیم فایل‌ها در مسیر `/var/www/idehpardaz` قرار دارند.

```bash
# ایجاد پوشه پروژه
mkdir -p /var/www/idehpardaz
```
*نکته: تمام فایل‌های سورس (src, package.json, vite.config.ts, ...) را در این پوشه آپلود کنید.*

## مرحله ۴: نصب وابستگی‌ها و بیلد کردن
به پوشه پروژه بروید و دستورات زیر را اجرا کنید:

```bash
cd /var/www/idehpardaz

# نصب پکیج‌های مورد نیاز
npm install

# ساخت نسخه نهایی (Build)
npm run build
```
بعد از اتمام این دستور، یک پوشه به نام `dist` ایجاد می‌شود که حاوی فایل‌های نهایی سایت است.

## مرحله ۵: تنظیم Nginx
حالا باید Nginx را تنظیم کنیم تا فایل‌های پوشه `dist` را نمایش دهد.

۱. یک فایل تنظیمات جدید بسازید:
```bash
sudo nano /etc/nginx/sites-available/idehpardaz
```

۲. محتویات زیر را در آن کپی کنید (به جای `YOUR_DOMAIN_OR_IP` دامنه یا آی‌پی سرور خود را بنویسید):

```nginx
server {
    listen 80;
    server_name YOUR_DOMAIN_OR_IP;

    root /var/www/idehpardaz/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # تنظیمات کش برای پرفورمنس بهتر
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }
}
```

۳. فایل را ذخیره کنید (Ctrl+O, Enter) و خارج شوید (Ctrl+X).

۴. سایت را فعال کنید:
```bash
sudo ln -s /etc/nginx/sites-available/idehpardaz /etc/nginx/sites-enabled/
```

۵. تنظیمات پیش‌فرض را غیرفعال کنید (اختیاری):
```bash
sudo rm /etc/nginx/sites-enabled/default
```

۶. تست تنظیمات و ری‌استارت Nginx:
```bash
sudo nginx -t
sudo systemctl restart nginx
```

## تبریک! 🎉
الان اگر آی‌پی سرور یا دامنه خود را در مرورگر وارد کنید، سایت باید بالا بیاید.

---

### نکته مهم درباره دیتابیس
این نسخه از پروژه از **LocalStorage** مرورگر برای ذخیره اطلاعات (پست‌ها، تنظیمات و...) استفاده می‌کند.
این یعنی:
۱. نیازی به نصب MySQL یا MongoDB **ندارید**.
۲. اگر کش مرورگر پاک شود، اطلاعات ادمین ریست می‌شود.
۳. اطلاعاتی که ادمین وارد می‌کند، برای کاربر دیگری نمایش داده نمی‌شود (چون دیتابیس سمت سرور ندارد).

**برای تبدیل این سایت به یک سایت واقعی با دیتابیس مرکزی:**
باید یک بک‌ند (با Node.js یا Python) بنویسید که جایگزین فایل `DataContext.tsx` شود و اطلاعات را از API بگیرد.
